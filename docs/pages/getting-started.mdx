# Getting Started

Velkommen til Nostromo UI! Denne guide hjælper dig med at komme i gang med vores UI bibliotek.

## Installation

Installer Nostromo UI i dit projekt:

```bash
npm install @nostromo/ui-core
```

## Grundlæggende Brug

```jsx
import { Button } from '@nostromo/ui-core'

export default function MyComponent() {
  return <Button>Klik mig</Button>
}
```

## Tailwind CSS

Nostromo UI bruger Tailwind CSS. Sørg for at have Tailwind konfigureret i dit projekt:

```bash
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```

## Næste Skridt

- Udforsk [komponenterne](/components/button) for at se alle tilgængelige komponenter
- Se [Storybook](/storybook) for interaktive eksempler
- Læs [API dokumentationen](/api) for detaljerede props

## Support

Har du spørgsmål? Tjek vores [FAQ](/faq) eller kontakt os på GitHub.
