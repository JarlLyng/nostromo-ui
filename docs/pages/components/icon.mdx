import StorybookEmbed from '../../components/StorybookEmbed'

# Icon

The Icon component provides you with icons and symbols for your application.

## Live Examples

<StorybookEmbed story="components-icon--default" />

<StorybookEmbed story="components-icon--sizes" />

<StorybookEmbed story="components-icon--colors" />

## Code Examples

```jsx
import { Icon } from '@nostromo/ui-core'

export default function IconExamples() {
  return (
    <div className="space-x-4">
      <Icon name="home" />
      <Icon name="user" />
      <Icon name="settings" />
      <Icon name="search" />
    </div>
  )
}
```

## Sizes

```jsx
import { Icon } from '@nostromo/ui-core'

export default function IconSizes() {
  return (
    <div className="space-x-4">
      <Icon name="star" size="sm" />
      <Icon name="star" size="default" />
      <Icon name="star" size="lg" />
    </div>
  )
}
```

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `name` | `string` | - | Icon name |
| `size` | `"sm" \| "default" \| "lg"` | `"default"` | Icon size |
