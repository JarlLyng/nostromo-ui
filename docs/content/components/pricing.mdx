import LiveCode from "../../components/LiveCode";

# Pricing

The Pricing component provides you with pricing tables and plans.

## Live Examples

<LiveCode code={`
import { Pricing } from '@jarllyng/nostromo'

export default function PricingExample() {
  return (
    <Pricing
      title="Choose Your Plan"
      subtitle="Flexible pricing for teams of all sizes"
      plans={[
        {
          id: 'starter',
          name: 'Starter',
          description: 'For side projects',
          price: { monthly: 9, yearly: 90 },
          period: 'month',
          features: [
            { id: 'projects', name: 'Up to 5 projects', included: true },
            { id: 'support', name: 'Basic support', included: true },
            { id: 'storage', name: '1 GB storage', included: true },
            { id: 'sso', name: 'SSO', included: false },
          ],
          cta: { text: 'Get Started' },
        },
        {
          id: 'pro',
          name: 'Pro',
          description: 'For growing teams',
          price: { monthly: 29, yearly: 290 },
          period: 'month',
          popular: true,
          badge: { text: 'Most popular' },
          features: [
            { id: 'projects', name: 'Unlimited projects', included: true },
            { id: 'support', name: 'Priority support', included: true },
            { id: 'storage', name: '10 GB storage', included: true },
            { id: 'sso', name: 'SSO', included: true },
          ],
          cta: { text: 'Start free trial' },
        },
      ]}
    />
  )
}
`} noInline={true} />

<LiveCode code={`import { Pricing } from '@jarllyng/nostromo'

export default function PricingComparisonExample() {
  return (
    <Pricing
      title="Compare Plans"
      variant="muted"
      columns={3}
      plans={[
        {
          id: 'basic',
          name: 'Basic',
          price: { monthly: 9 },
          period: 'month',
          features: [
            { id: 'projects', name: '5 projects', included: true },
            { id: 'support', name: 'Basic support', included: true },
            { id: 'advanced', name: 'Advanced features', included: false },
          ],
          cta: { text: 'Get Started' },
        },
        {
          id: 'pro',
          name: 'Pro',
          price: { monthly: 29 },
          period: 'month',
          popular: true,
          features: [
            { id: 'projects', name: 'Unlimited projects', included: true },
            { id: 'support', name: 'Priority support', included: true },
            { id: 'advanced', name: 'Advanced features', included: true },
          ],
          cta: { text: 'Get Started' },
        },
        {
          id: 'enterprise',
          name: 'Enterprise',
          price: { monthly: 99 },
          period: 'month',
          features: [
            { id: 'pro', name: 'Everything in Pro', included: true },
            { id: 'support', name: 'Dedicated support', included: true },
            { id: 'custom', name: 'Custom integrations', included: true },
          ],
          cta: { text: 'Contact Sales' },
        },
      ]}
    />
  )
}`} noInline={true} />

<LiveCode code={`import { Pricing } from '@jarllyng/nostromo'

export default function PricingSimpleExample() {
  return (
    <Pricing
      title="Simple Pricing"
      columns={2}
      plans={[
        {
          id: 'monthly',
          name: 'Monthly',
          price: { monthly: 29 },
          period: 'per month',
          features: [
            { id: 'all', name: 'All features', included: true },
            { id: 'email', name: 'Email support', included: true },
          ],
          cta: { text: 'Start Free Trial' },
        },
        {
          id: 'yearly',
          name: 'Yearly',
          description: 'Save $58 per year',
          price: { monthly: 24, yearly: 290 },
          period: 'per year',
          badge: { text: 'Best Value', variant: 'success' },
          features: [
            { id: 'all', name: 'All features', included: true },
            { id: 'priority', name: 'Priority support', included: true },
            { id: 'save', name: 'Save 20%', included: true },
          ],
          cta: { text: 'Start Free Trial' },
        },
      ]}
    />
  )
}`} noInline={true} />

## Import

```tsx
import { Pricing } from "@jarllyng/nostromo";
// or
import { Pricing } from "@jarllyng/nostromo/pricing";
```

## Usage

```tsx
import { Pricing } from "@jarllyng/nostromo";

export default function Example() {
  return (
    <Pricing
      title="Choose Your Plan"
      plans={[
        {
          name: "Starter",
          price: "$9",
          period: "month",
          features: ["Feature 1", "Feature 2"],
        },
      ]}
    />
  );
}
```

## Code Examples

## Props

| Prop              | Type                                      | Default     | Required | Description                                              |
| ----------------- | ----------------------------------------- | ----------- | -------- | -------------------------------------------------------- |
| `plans`           | `PricingPlan[]`                           | -           | **Yes**  | Array of pricing plans (see PricingPlan interface below) |
| `title`           | `string`                                  | `undefined` | No       | Section title                                            |
| `subtitle`        | `string`                                  | `undefined` | No       | Section subtitle                                         |
| `columns`         | `1 \| 2 \| 3 \| 4`                        | `3`         | No       | Number of columns in grid                                |
| `variant`         | `"default" \| "featured" \| "comparison"` | `"default"` | No       | Visual variant                                           |
| `showYearly`      | `boolean`                                 | `false`     | No       | Whether to show yearly billing toggle                    |
| `onToggleBilling` | `(yearly: boolean) => void`               | `undefined` | No       | Callback when billing period toggles                     |
| `className`       | `string`                                  | `undefined` | No       | Additional CSS classes                                   |

### PricingPlan Interface

```tsx
interface PricingPlan {
  name: string;
  price: string;
  period?: string;
  description?: string;
  features: string[];
  cta?: React.ReactNode | string;
  popular?: boolean;
  badge?: string;
}
```
